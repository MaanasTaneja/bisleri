# ContextKit Tickets

The full project is split into GitHub issues:

1. Project scaffold and developer docs
2. Python MCP server skeleton
3. Local memory store and embeddings
4. Core MCP tools
5. Ingest HTTP API
6. SwiftUI menu bar app shell
7. Swift capture and ingestion pipeline
8. Server process and client integration managers
9. Privacy controls
10. Verification harness

This first implementation pass completes the scaffold, local Python server, core tool logic, ingest API, Swift source skeleton, privacy/server managers, and Python verification harness. Native macOS build validation still needs to happen on macOS with Xcode.
